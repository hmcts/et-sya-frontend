{% extends "template.njk" %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}

{% block beforeContent %}
  {{ super() }}
  {{ govukBackLink({
  text: back,
  href: "/"
}) }}
{% endblock %}

{% block content %}
    <div class="govuk-panel--informational">
        <h1 class="govuk-panel__title">{{ h1 }}</h>
      <div class="govuk-panel__body">
          <p>{{ p1 }}</p>
          <p>{{ p2 }}</p>
      </div>   
    </div>

{% endblock %}