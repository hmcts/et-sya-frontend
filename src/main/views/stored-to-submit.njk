{% extends "template.njk" %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}

{% block beforeContent %}
  {{ super() }}
  {% include "back-link.njk"%}
{% endblock %}

{% block content %}
  <div class="govuk-!-width-two-thirds">

    <h1 class="govuk-heading-xl">{{applicationType}}</h1>
    <p class="govuk-body">{{youHaveNotSubmitted}}</p>
      {{ govukSummaryList({
        rows: appContent
      }) }}

    <h2 class="govuk-heading-l">{{confirmYouHaveCopied}}</h2>
    <p class="govuk-body">{{toSubmitYourApplication}}</p>
    <p class="govuk-body"><a href="{{thisCorrespondenceLink}}" class="govuk-link">{{viewThisCorrespondence}} ({{opensInNewTab}})</a></p>
    <p class="govuk-body">{{viewTheSupportingDocuments}} <br/><a href="{{thisCorrespondenceFileLink}}" class="govuk-link">{{thisCorrespondenceFileName}} ({{opensInNewTab}})</a></p>

    <h3 class="govuk-heading-m">{{haveYouCopied}}</h3>
    <p class="govuk-body">{{iConfirmThatIHaveCopied}}</p>

    {% block form %}
      {% include "form/form.njk"%}
    {% endblock %}

  </div>

  </div>
{% endblock %}
